import styled from 'styled-components';
import { Colors, BoxShadow } from '@atomic/atm.constants';

export const AppHeaderStyled = styled.div`
  background-color: ${Colors.White};
  box-shadow: ${BoxShadow.Depht2};
`;
